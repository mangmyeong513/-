
{
  "name": "file-db-backend",
  "version": "1.0.0",
  "description": "Tiny file-based backend to replace Replit DB (JSON per table).",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "init": "node scripts/init.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nanoid": "^5.0.7"
  }
}
